# Preferred key:
intervention_date: "2021-02-01"

# Legacy is still honored if present:
# policy_date: "2021-02-01"

# Your official dataset:
data_path: "data/ontario_cases.csv"

# Optional: if you plan to drive PSM with explicit covariates
# covariates: ["pre_level", "pre_trend"]
