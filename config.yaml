# === DATA MAPPING (EDIT THESE TO MATCH YOUR CSV) ===
data_path: data/ontario_cases.csv       # point to the cleaned COVID‑19 cases CSV
date_col: week                          # weekly period column
unit_col: region                        # public health unit identifier
outcome_col: incidence                  # weekly mean of active cases
treat_col: treated                      # 1=treated unit(s), 0=control
covariates: []                          # optional covariates (none in this dataset)

# === STUDY DESIGN ===
intervention_date: 2021-10-15             # set to your policy start date (YYYY-MM-DD)
freq: W                                   # 'W' weekly, 'D' daily, etc.
